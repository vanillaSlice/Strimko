package lowe.mike.strimko.controller;

public class Controller {

	// load view --- this loads panel on left etc
	// modes - play/create

	// when user clicks drop downs -load file listings and repopulate
	// when user clicks puzzle call file puzzle load and load puzzle instance
	// pass view size of grid, streams ie colours to set up and where to put
	// numbers
	// load numbers etc

	// MODEL DOESN'T INTERACT WITH CONTROLLER
	// VIEW DOESN'T

	// create mode
	// loads empty grid
	// allows user to set size, numbers, streams (automatically for strimko)
	// create passes grid into grid validator
	// then passes to solver and write to file
	// reset view
}